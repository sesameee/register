export default interface formDataVO {
    mobile: string;
    code: string,
    // eslint-disable-next-line @typescript-eslint/camelcase
    sms_code: string,
    password: string,
    repassword: string,
}
